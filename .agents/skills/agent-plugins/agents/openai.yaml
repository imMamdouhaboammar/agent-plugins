interface:
  display_name: "Agent Plugins"
  short_description: "Repo-specific patterns and workflows for agent-plugins"
  default_prompt: "Use the agent-plugins repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true